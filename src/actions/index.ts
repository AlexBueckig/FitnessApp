/*const REQUEST = 'REQUEST';
const SUCCESS = 'SUCCESS';
const FAILURE = 'FAILURE';

function createRequstTypes(base) {
  return [REQUEST, SUCCESS, FAILURE].reduce((acc, types) => {
    acc[type] = `${base}_${type}`;
    return acc;
  }, {});
}
*/
